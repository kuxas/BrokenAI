# abc086_c

自力で書いたコードの供養.おしいところまで言っていたがミスがいくつかあったため動かず...
・この問題の本質を理解できていなかった.
→この問題で問われている条件は2つ.
    ・移動距離 <= 時間差
    ・時間差と距離の偶奇の一致
自分は偶奇の一致のみ考えており、移動距離 <=　時間差の条件を見落としていた.
・移動距離 <= 時間差について
    距離の認識が間違っていた.距離 >= 0となる必要があるため,絶対値をとる必要がある.

これまで解いてきた印象として,初期条件の見落とし,問題に問われている内容の本質を考えないまま
解き進めている.
とりあえず書き始めることも大切だとは思うが,次回は条件をしっかりと考察したうえで書き進めていく.

int sum_xy(vector<vector<int>> a, int i) {
  int x = a[i][1];
  int y = a[i][2];
  return x + y;
}



int main() {
  int n;
  cin >> n;
  vector<vector<int>> a(n, vector<int>(3));

  rep (i, n) {
    rep (j, 3) {
      cin >> a[i][j];
    }

    int t = a[i][0];
    if (i == 0) {
      if ((t - sum_xy(a, i)) % 2 != 0) {
        cout << "No\n";
        return 0;
      }
    }
    else {
      t -= a[i - 1][0];
      if ((t - sum_xy(a, i)) % 2 != 0) {
        cout << "No\n";
        return 0;
      }
    }
  }
  
  cout << "Yes\n";
  return 0;
}
  
*/